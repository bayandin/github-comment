{
  "name": "github-comment",
  "version": "0.0.0",
  "description": "Add a comment to a GitHub PR",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
